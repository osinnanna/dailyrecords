let date: string = "2026-12-04 2:00:00";
const hello = new Date(date).toLocaleString();

console.log(hello);

// This is to know how the user would be able to insertData in the case that the PI is off or something went wrong with the localAreaNetwork.
